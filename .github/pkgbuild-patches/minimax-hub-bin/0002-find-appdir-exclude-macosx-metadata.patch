--- a/PKGBUILD
+++ b/PKGBUILD
@@ -78,7 +78,7 @@
 
   7z x -bd -y -snl- "${archive_file}" -oupstream-app >/dev/null
 
-  appdir="$(find upstream-app -path '*/MiniMax Hub.app' -type d -print -quit)"
+  appdir="$(find upstream-app -path '*/MiniMax Hub.app' -not -path '*/__MACOSX/*' -type d -print -quit)"
 
   [[ -n "${appdir}" && -d "${appdir}" ]] || {
     echo "Could not find MiniMax Hub.app in upstream archive" >&2
